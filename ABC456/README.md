# ABC456

## C - Not Adjacent

给定一个由 `a`, `b`, `c` 组成的字符串 `S`。

求 `S` 的空でない部分文字列（子串）中，没有两个相邻字符相同的个数，对 998244353 取模。

两个子串如果取自不同的位置，即使字符串相同也视为不同。

**约束:**
- `S` 的长度为 1 到 3×10^5

**输入:**
```
S
```

**输出:**
答案

---

## D - Not Adjacent 2

给定一个由 `a`, `b`, `c` 组成的字符串 `S`。

求 `S` 的空でない部分列（子序列）中，没有两个相邻字符相同的个数，对 998244353 取模。

两个子序列如果取自不同的位置，即使字符串相同也视为不同。

**约束:**
- `S` 的长度为 1 到 3×10^5

**输入:**
```
S
```

**输出:**
答案
